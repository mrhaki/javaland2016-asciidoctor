apply plugin: 'groovy'

repositories {
    jcenter()
}

dependencies {
    compile 'org.asciidoctor:asciidoctorj:1.5.2', 'org.kordamp.gipsy:gipsy:0.4.0'
    compile gradleApi()
}

configurations {
    compile {
        exclude group: 'org.codehaus.groovy', module: 'groovy-all'
    }
}
